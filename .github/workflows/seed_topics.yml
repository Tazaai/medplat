name: Seed Firestore topics (manual)

on:
  workflow_dispatch:

jobs:
  seed:
    name: Seed topics into Firestore
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4

      - name: Use Node 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install backend deps
        run: npm --prefix backend ci

      - name: Seed topics
        env:
          FIREBASE_SERVICE_KEY: ${{ secrets.FIREBASE_SERVICE_KEY }}
          TOPICS_COLLECTION: topics2
        run: |
          if [ -z "${FIREBASE_SERVICE_KEY:-}" ]; then
            echo "FIREBASE_SERVICE_KEY not set — aborting"
            exit 1
          fi
          node scripts/seed_topics.mjs
name: Seed Firestore topics (manual)

on:
  workflow_dispatch: {}

jobs:
  seed:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install firebase-admin for seeder
        run: |
          npm --prefix backend install firebase-admin --no-audit --no-fund

      - name: Seed topics
        env:
          FIREBASE_SERVICE_KEY: ${{ secrets.FIREBASE_SERVICE_KEY }}
          TOPICS_COLLECTION: topics2
        run: |
          # Run the ESM seeder (scripts/seed_topics.mjs) which itself checks for the secret
          node scripts/seed_topics.mjs
