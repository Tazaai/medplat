“Project: Medplat — a global, dynamic AI platform that generates realistic medical cases across all specialties with gamification, adaptive difficulty, expert reasoning, and structured outputs. Backend: Node/Express on Cloud Run; use POST only and load the OpenAI key from Secret Manager (never from frontend). API routes to respect: /api/dialog (should return { ok, case, text }), /api/topics, /api/topics/categories, /api/gamify (expects a text field and honors must_include), and /api/comment. Firestore structure: collection topics2 (and peers) with docs { category, id (snake_case), topic (display), lang (ISO-639-1; default 'en') } — no niveau; multi-language supported with English as default. Ensure CORS allows our frontend origin. All improvements must be global (apply to all categories/topics), not one-off case hacks.
Collab rules: Do not edit code automatically. First analyze/diagnose and propose exact changes (diffs/patches with line numbers). Apply only after we explicitly reply APPROVED. Provide quick curl/Vite test steps and never expose secrets