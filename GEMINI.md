Got it. Here’s the trimmed **one‑liner for Gemini** (routes + CORS removed):

“**Project: Medplat** — a global, dynamic AI platform that generates realistic medical cases across all specialties with gamification, adaptive difficulty, expert reasoning, and structured outputs. **Backend:** Node/Express on **Google Cloud Run (GCP)**, developed via **Cloud Workstations**; **POST‑only** APIs; load the OpenAI key from **Secret Manager** (never from frontend). **Firestore:** collection `topics2` (and peers) with docs `{ category, id (snake_case), topic (display), lang (ISO‑639‑1; default 'en') }` — **no `niveau`**; multi‑language supported with English as default. **Scope:** improvements must be **global** (apply to all categories/topics), not one‑off case hacks. **Collab rules:** do **not** edit code automatically; first analyze/diagnose and propose exact changes as diffs/patches with file paths and line numbers; apply only after we explicitly reply **APPROVED**; provide quick `curl`/Vite test steps; never expose secrets.”
