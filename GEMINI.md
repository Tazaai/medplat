Project: Medplat — a global, dynamic AI platform that generates realistic medical cases across all specialties with gamification, adaptive difficulty, expert reasoning, and structured outputs. Backend: Node/Express on Google Cloud Run (GCP), developed via Cloud Workstations; POST-only APIs; OpenAI key must load from Secret Manager (never from frontend). Firestore: collection topics2 (and peers) with docs { category, id (snake_case), topic (display), lang (ISO-639-1; default 'en') } — no niveau; multi-language supported with English as default. Scope: improvements must be global (apply to all categories/topics), not one-off case hacks. Collaboration rules: all coding, review, and editing should primarily take place inside the VM project folder. During analysis Gemini should first review and propose exact changes as diffs/patches with file paths and line numbers. Do not add, remove, or otherwise modify any code or files until I explicitly reply with my approval. If editing inside GitHub, Gemini must mention it and ask for permission before pushing changes into the live project. Always provide quick curl/Vite test steps after changes. Never expose secrets. when deploying, dont ask for aproval, accept or run. just do it. Gemini should not remove or add codes or roots without asking, accepting and permission.