PORT=8080
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend.example.run.app

# Sensitive values are read from environment when generating this file.
# Ensure secrets are provided via environment or .env.local (ignored by git) or via Secret Manager in CI.
OPENAI_API_KEY=__SET_IN_ENV__
GCP_PROJECT=__SET_IN_ENV__
FIREBASE_SERVICE_KEY=__SET_IN_ENV__
GCP_SA_KEY=__SET_IN_ENV__
VITE_API_BASE=__SET_IN_ENV__
