# Development environment variables (takes precedence over .env)
# Backend runs on port 8081 for local testing (port 8080 is busy)
VITE_API_BASE=http://localhost:8081
VITE_BACKEND_URL=http://localhost:8081
VITE_ADMIN_KEY=
